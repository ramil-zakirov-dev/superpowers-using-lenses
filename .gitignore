# Secrets — never committed. See .env.example for the shape.
.env

# Derived artifacts. The catalog YAML is source; the index is rebuilt from it.
index/

# Model weights fetched by sentence-transformers (HF cache) or downloaded by
# hand — never vendored content, never committed.
*.gguf

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.pytest-tmp/
